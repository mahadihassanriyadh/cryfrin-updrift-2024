# Some Go To Commands I Need For Security Research Setup Regularly
## Cloning a repo at a specific commit
```bash
git clone <repo-url> --branch <branch-name> --single-branch
git reset --hard <commit-hash>
```